extends layout

block content
  h1(style='margin-top:100px;') #{translation.GenreList}
  
  ul
  each val in list_genres
    li 
      a(href=val.url) #{val.name}

  else
    li #{translation.nogenres}
